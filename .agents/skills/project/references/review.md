# REVIEW.md

根目录常驻。PR 评审策略，对照本次 `docs/sdlc/changes/<短名>/` 的 intent / spec / plan。

建议三段 pass：Bugs、Security、Compliance（是否匹配 spec.md、plan.md、AGENTS.md 硬规则）。

Important 只留给会破坏行为、漏数据或破硬规则的发现。Nit 封顶。CI 已覆盖的不要再报。
