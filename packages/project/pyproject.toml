[project]
name = "coreskills-project"
version = "0.1.0"
description = "Project structure check, document-status check, and git hooks that prompt doc updates"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
project = "coreskills_project.cli:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
